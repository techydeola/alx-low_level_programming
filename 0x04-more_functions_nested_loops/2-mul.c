#include "main.h"

/**
 * mul - is a funtion that multiplies two integers
 * @a: the first argument
 * @b: the second argument
 *
 * Return: the result
 */

int mul(int a, int b)
{
	return (a * b);
}
