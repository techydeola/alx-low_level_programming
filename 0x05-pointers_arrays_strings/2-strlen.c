#include "main.h"
#include <string.h>

/**
 * _strlen - a function that returns lentht of string
 * @s: pointer to be checked
 *
 * Return: len
 */

int _strlen(char *s)
{
	size_t len = strlen(s);

	return (len);
}
