#include "main.h"

/**
 * add - adds the two arguments passed in
 * @a: the first argument
 * @b: the second argument
 *
 * Return: The result of the function
 */

int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
